{
  "name": "toolbelt",
  "version": "0.1.0",
  "description": "Coding Toolbelt CLI — skill, hook, agent, and command management for AI coding agents",
  "type": "module",
  "bin": {
    "toolbelt": "./bin/toolbelt"
  },
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "bun build --compile --target=bun-darwin-arm64 src/index.ts --outfile bin/toolbelt",
    "build:linux": "bun build --compile --target=bun-linux-x64 src/index.ts --outfile bin/toolbelt-linux-x64",
    "install:local": "bun run build && ln -sf $(pwd)/bin/toolbelt $HOME/.local/bin/toolbelt && echo 'Linked to ~/.local/bin/toolbelt'",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0"
  }
}
